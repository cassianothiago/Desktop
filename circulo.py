from shape import*
import math
class Circulo(Shape):
    def __init__(self, cor, raio):
        super().__init__(cor)
        self.base=raio
        
        def calculo(self,raio):
            self.area=math.pi*raio
            return self.area
            
            
        